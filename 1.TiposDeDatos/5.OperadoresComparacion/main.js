// Operadores de Comparacion

// Igualdad (==) Compara dos valores y los convierte si es necesario.

// Estrictamente igual (===) Compara dos valores y sus tipos, sin convertir.

// Desigualdad (!=) Comprueba si dos valores son diferentes, convirtiendo tipos.

// Desigualdad estricta (!==) Comprueba si dos valores o tipos son diferentes, sin convertir.

// Mayor que (>) Verifica si el valor de la izquierda es mayor que el de la derecha.

// Mayor o igual que (>=) Verifica si el valor de la izquierda es mayor o igual al de la derecha.

// Menor que (<) Verifica si el valor de la izquierda es menor que el de la derecha.

// Menor o igual que (<=) Verifica si el valor de la izquierda es menor o igual al de la derecha.


const a = 10 
const b = 20
const c = "10"

console.log(a>c);

/* Consejo si queremos comparar algo en javascript es mejor usar el tipo de dato que sea estrictamente para que nos de el lado falso
Porque en JavaScript si tenemos un 10 que es Number y un "10" String. El va a decir que son iguales    
*/


